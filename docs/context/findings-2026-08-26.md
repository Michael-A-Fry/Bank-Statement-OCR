# Investigation findings, 26 Aug 2026

Thirteen faults from docs/context/outstanding-work.md, each root-caused against the
real code and REPRODUCED. Exact edit sites. The adversarial pass was cut short to
start implementing, so treat a "medium" confidence as needing a check on the way in.


================================================================
A2-variable-table-count [high] REPRODUCED
ROOT CAUSE: A table entry is a COORDINATE, not a description. `doc_locate_table()` (R/tables.R:437) resolves exactly one page - `p0 <- .doc_int(tab$start$page)`, clamped into range at R/tables.R:441 - reads the lines of that one page (R/tables.R:464), asks `.doc_find_header()` for the ONE best line on it (R/tables.R:386-394, `which.max`), and returns exactly one window set. The heading wording and the column bands the template already carries are used only to refine a location that has already been decided by `start.page`. Every symptom is that single fact: a second occurrence elsewhere is never looked for; "however many" cannot be expressed; and when `start.page` is past the end of this copy the clamp silently retargets the search at the last page and the position fall-through (R/tables.R:486-491, confidence 0.4) reads whatever ink is there through this table's bands - a wrong number that looks right.
FIX: ONE optional key, borrowed word-for-word from the fields mode: `occurrence: all` on a table entry. Absent (or `once`/`first`) = today's code path, byte for byte, so every saved template is untouched. What a repeating entry declares instead of coordinates is NOTHING NEW - it already carries `anchor.header_text` (its heading wording) and `columns` (its band shape), and those two together are the match rule .doc_continues already trusts. `start`/`end` stay on the entry as the example location (the round-trip back to the builder needs them) and simply stop being the search space.

MATCHING RULE. A line, on any page, is an occurrence when all three hold: (a) .doc_header_hit >= 0.75 against anchor.header_text - the same bar the follow loop already uses at R/tables.R:539, raised from 0.6 because every line of every page is now a candidate; (b) it puts a word in at least half the declared column bands (min 2) - the shape half of .doc_same_shape, asked of declared bands; (c) it breaks into at least that many gutter-separated cells (.doc_cells) - the ncell test from tables_detect.R:97, which is what kills prose that merely names the columns (measured: without (c) a sentence "Refer to the Security Units Cost Market value table" scored as an occurrence; with it, zero). Then the EXISTING machinery takes over unchanged: the window opens at the header top, band$y_max closes it, follow walks continuation pages, the row-run rule ends it within a page. A header top already inside a claimed window is that occurrence's continuation, not a new one - which is why the fixture's 3-page transaction table still reads as ONE table of 83 rows.

OUTPUT WHEN THERE ARE SEVEN. Seven occurrences are ONE table with ONE column set - they must be, that is the match rule - so there is no name collision to solve and no seven sheets. The rows are concatenated in document order under the template's single name, `row` numbered continuously, and a new leading `section` column carries the heading printed above each occurrence ("Fund A holdings" ... "Fund G holdings"). `section` is added ONLY for an `occurrence: all` table, so no existing template's output shape moves. .doc_sheet_names is untouched. The person sees one sheet, one header row, a Section column and the Page column they already have.

ABSENT, NOT EMPTY. Zero occurrences means the key is never assigned in extract_document's loop: no entry in ext$tables, no sheet, no rows in long, no per-column report. It is STATED, not silent - extract_document returns a `misses` vector, document_summary appends one row per miss (rows 0, found_by "not on this document"), and convert_tables reports them as `absent_tables` in the message. Absence does NOT demote status for an `occurrence: all` table - three-this-month-seven-next means absence is the expected case and a permanent needs_review is a warning nobody reads - but a MISSING pinned (non-repeating) table stays in empty_tables -> needs_review exactly as today.

TWO DEFECTS IN THE SAME MECHANISM, FIXED IN THE SAME PASS, BOTH SERVING TODAY'S TEMPLATES TOO. (i) The clamp at R/tables.R:441 must not fabricate: `p0 > npg` returns zero windows, anchor "not_found", confidence 0 - which turns the 8 invented position rows into an honest empty and a needs_review. (ii) The follow loop must carry .doc_continues's own-heading refusal, or a repeating table swallows the next one (measured 53 rows where 48 are right).

THE BUILDER, WITHOUT ANYONE READING YAML - AND IT REMOVES CONTROLS RATHER THAN ADDING THEM. propose_tables already produces seven near-identical entries called "Fund A holdings", "Fund A holdings 1"... Collapse them at propose time using the .doc_same_header + .doc_same_shape pair that is already in the file: seven entries become ONE, carrying occurrence = "all". Measured: the 7-fund pack goes from 7 entries on screen to 1; the fixture goes from 6 to 5 and "Account summary 1" disappears. The only new thing on screen is a line in the muted subtitle output$rb_saved already prints - "7 places <c2><b7> pages 3, 8, 13, 18, 23, 28, 33" - and one small button in the Edit/Remove row that already exists: "Only this one". Net: six fewer things to read, one more button on the single entry that repeats. For a table drawn by hand, rb_savetab runs the same scan on the draft and, if it finds more, sets occurrence and says so in the notification it already shows. No dialog, no new screen, no question the tool could have answered.
   - R/tables.R @ .doc_find_header <- function(lines, need, min_hit = 0.6) {  (line 386)
       Add three small siblings beside it. `.doc_find_headers(lines, need, min_hit = 0.75)` - every line at or above the bar, not which.max, returning list(index, top, hit). `.doc_heading_above(lines, i, pitch)` - the heading printed above a line, lifted verbatim from tables_detect.R:193-200 so the propose
   - R/tables.R @ p0 <- min(p0, max(npg, 1L)); p1 <- min(p1, max(npg, 1L))  (line 441)
       THE CARDINAL FIX. Before the clamp: if `p0 > npg`, return immediately with windows = list(), pages = integer(0), anchor = "not_found", confidence = 0, detail = sprintf("page %d is past the end of this document, which has %d page(s)", p0, npg). Keep the p1 clamp - a table declared to end on page 7 in
   - R/tables.R @ if (!isFALSE(tab$follow) && p1 < npg && y_end_declared >= band_bot - 1) {  (line 535)
       Inside the while loop, after `hp <- .doc_find_header(lp, need_hdr, min_hit = 0.75)` and before accepting page p: if .doc_heading_above(lp, hp$index, pitch) is not NA, `break` - this page starts a table of its own. This is .doc_continues's `if (isTRUE(cd$.own_heading)) return(FALSE)` (tables_detect.R
   - R/tables.R @ the end of doc_locate_table, immediately above the '# Columns' banner (lines 562-566)
       New `doc_locate_repeats(input, tab, tmpl, cache = NULL)` -> list of loc, one per occurrence, each with an extra `$section`. Walk the cached pages in order; for each .doc_find_headers hit apply the band test then the .doc_cells test; skip a hit whose top already falls inside a claimed window; build a
   - R/doc_extract.R @ extract_document, the `for (k in keys) {` loop (lines 271-278)
       Build the page cache once above the loop. Inside: `occ <- as.character(tab$occurrence %||% "once")[1]`; if occ == "all", call doc_locate_repeats - zero results append k to a `misses` vector and `next` (ABSENT: no entry, so no sheet, no long rows, no report rows); one or more results are read with do
   - R/doc_extract.R @ document_summary <- function(tables) {  (line 287)
       Take a second argument `misses = character(0)` (and the table specs, for the names). After the per-table rows, append one row per miss: rows 0L, columns 0L, pages "", found_by "not on this document", confidence NA, note "this table is not on this copy - it is set to be read wherever it appears, and 
   - R/doc_extract.R @ document_long, `vals <- setdiff(names(r), c("page", "row"))`  (line 356)
       Exclude "section" from the melt (`c("page", "row", "section")`) and carry it as a real column of the long frame beside `table_name`, NA for a non-repeating table. Otherwise the fund name melts into the value column and the long file loses which fund every figure belongs to. Update the `empty` protot
   - R/doc_extract.R @ .doc_table_problems <- function(tab, key) {  (line 49), after the columns loop
       The whole validation delta, firing only on the new key: reject an `occurrence` that is not once/first/all; and for `all`, require at least two non-empty entries in anchor.header_text - "it is set to be read everywhere it appears, but it has no heading wording to be found by; without that there is no
   - R/doc_extract.R @ document_template_from_proposal, `keyed[[k]] <- list(name = ..., band = .doc_band_of(tb), header_rows = ..., follow = ...)`  (lines 627-652)
       Add `occurrence = as.character(tb$occurrence %||% "once")[1]` to the explicit key list. This is the SAME drop site the `band` comment at lines 634-641 documents - a key set on the draft, shown on screen, and then silently not written, so the reader never sees it. And in document_proposal_from_templa
   - R/tables_detect.R @ propose_tables, `if (!joined) merged[[length(merged) + 1L]] <- cd`  (line 311), and the follow-stamping lapply at line 316
       After continuation merging, collapse: for each candidate look back over the kept list for one where .doc_same_header(o, cd) AND .doc_same_shape(o, cd); on a hit set `o$occurrence <- "all"`, add cd's start to `o$.places`, and sum n_rows instead of appending a new entry. Two headerless tables can neve
RISK: Measured cost: 0.24 s per repeating table on a 40-page pack with the shared page cache (0.39 s without it), so 7.2 s if all thirty tables of a pack were marked `all`. Only tables carrying the key pay it, and the cache must be built once in extract_document, not once per table - the implementation pa

================================================================
A1-continuation-headers [high] REPRODUCED
ROOT CAUSE: On a continuation page, exactly ONE thing closes the top of the reading window: a >=0.6 wording match of the template's remembered `anchor$header_text` against a SINGLE printed line (R/tables.R:508 calling .doc_find_header, R/tables.R:386). There is no second answer. When that one test misses, R/tables.R:500 falls back to `band$y_min` - which nothing in the product ever writes, so it is 0, the very top of the page - and R/tables.R:505 hard-codes `skip` to 0 for every page after the first. The window is then "the whole page", and every line printed above the data, the reprinted column heading included, is read as a row. The commonest way to miss is a table with no figures in it: the proposer marks such a table has_header = FALSE, so it writes `header_rows: 0` and `anchor.header_text: []` (R/tables_detect.R:182-190), the wording test has nothing to match and can never fire, and setting "Header lines" in the builder fixes page 1 only, because `skip` is applied to p0 alone.
FIX: Stop asking the template what the heading says and ask the DOCUMENT. On the table's own first page the tool already knows which lines are the heading - they are the `skip` lines at the top of that window. Take their text as the heading's signature for THIS document (normalised via .doc_norm, one string per header line). On every later page, if the existing wording test misses, look for a line whose whole normalised text equals or contains that signature; where it is found, open the window at that line and skip as many lines as actually matched. No new control, no new template key, nothing on screen to answer: the signature is learned per document at read time, so it is right even when the template was drawn on a different copy. Two guards keep it from ever deleting a real row - the signature is refused if any learned header line carries a figure (.doc_has_money), and the match is whole-line, not a fraction, so a data row cannot contain the entire heading string. And it is reported: the skip is already counted in n_header, and loc$detail gains a sentence naming the pages, which document_summary already surfaces as the `note` column (R/doc_extract.R:314).
   - R/tables.R @ immediately after .doc_find_header(), which ends at line 394 (before the .doc_find_first_column comment block at 396)
       Add two helpers. `.doc_header_printed(lines, y_from, n)`: take the first `n` lines at or below `y_from`; return character(0) if ANY of them satisfies .doc_has_money(d) (defined R/tables_detect.R:77, sourced alongside); otherwise return their .doc_norm(.doc_line_text(d)), dropping any shorter than 6 
   - R/tables.R @ in doc_locate_table(), at the close of the anchor if/else block (line 493), just before the comment '# A found header on the start page also tells us...' at line 495
       Add: hdr_sig <- if (start_at_header) .doc_header_printed(lines0, y_start, n_header_rows) else character(0). This is the heading as THIS document prints it.
   - R/tables.R @ line 508, `hp <- .doc_find_header(lp, need_hdr)` inside the `if (p > p0)` branch
       After it add: if (is.null(hp)) hp <- .doc_find_printed_header(lp, hdr_sig). Then change line 511 from `skip <- n_header_rows` to `skip <- .doc_int(hp$n_lines, n_header_rows)`, so a page that reprints only the first line of a two-line heading cannot lose a data row.
   - R/tables.R @ line 539, `hp <- .doc_find_header(lp, need_hdr, min_hit = 0.75)` in the FOLLOW loop
       After it add the same fallback: if (is.null(hp)) hp <- .doc_find_printed_header(lp, hdr_sig). And change the window's `skip = as.integer(n_header_rows)` at line 543 to `skip = .doc_int(hp$n_lines, n_header_rows)`. A whole-line signature match is a STRONGER signal than a 0.75 wording hit, so followin
   - R/tables.R @ lines 557-558, the `if (!is.na(extended_to)) detail <- paste0(detail, ...)` sentence
       After it append the reprint sentence from header_tops (dead today): rep_pages <- setdiff(as.integer(names(header_tops)), p0); if (length(rep_pages)) detail <- paste0(detail, sprintf('; its heading is printed again on page%s %s and is not read as rows', if (length(rep_pages) == 1L) '' else 's', paste
   - R/tables.R @ lines 559-561, the returned list of doc_locate_table(), and its header comment at 431-432
       Add header_sig = hdr_sig to the returned list and name it in the comment.
   - R/tables.R @ lines 1091-1094 in doc_table_rows(), the `is_header <- li <= skip || (.doc_header_hit(...) >= 0.6 && !any(...money...))` expression
       Add a third way, using the signature now carried on loc: `|| (length(loc$header_sig) && !is.null(.doc_find_printed_header(list(d), loc$header_sig)))`, inside the same no-money guard that already protects a totals row echoing the column names. Belt-and-braces for a heading reprinted at a y the window
RISK: Low, and bounded to R/tables.R. The new path only ever runs when the existing wording test has already returned NULL, so every template that works today takes the identical code path - confirmed by 1581 assertions across the document surface passing unchanged with the prototype sourced over the engi

================================================================
A3-blank-first-row [medium] REPRODUCED
ROOT CAUSE: The column DIVISIONS are derived from the ink that happens to be inside the sample the gesture caught, and the derived bands TILE (each band is widened to the midpoint of the gutter, so neighbours share an edge). doc_columns_from_box() takes `bands <- .doc_bands(sel, gap)` over every word in the drag box (R/tables.R:916) and .doc_bands widens each band to the midpoint of the white space on each side (R/tables_detect.R:67-71). A column that prints nothing inside that sample therefore yields NO band, and because the surviving bands meet, its whole territory belongs to a neighbour - so every value it holds on every other row is read into the neighbouring cell. It is silent by construction: a tiled column claims every word, so doc_table_rows()'s unclaimed-word list (R/tables.R:1185-1189) is empty and the vanished column is not in the fill report at all. A blank first row is one way of emptying that sample; the same mechanism narrows a band when the row is blank but the heading is present, and propose_tables() (R/tables_detect.R:300-309) keeps only the FIRST page's bands when it joins a continuation, which is the same defect over a different sample.
FIX: One rule: THE DIVISIONS COME FROM ALL THE INK IN THE TABLE, NOT FROM THE SAMPLE THE GESTURE CAUGHT - and a division that any single line shows is a division. Two halves, both made of pieces that already exist. (1) Inside doc_columns_from_box(), the drag box goes back to doing what its contract says: it NAMES the columns and says where the heading is; before the bands are turned into columns, any band that contains two or more cells of ANY line in the box is split at the midpoint between those cells (the function already computes `cells`). That restores a column whose heading exists but whose blank first row let its band merge into a neighbour. (2) In the builder's "cols" handler, the moment the table's end is known (doc_auto_end, the very next line), the divisions are re-derived once over the WHOLE table with doc_fit_columns(), keeping the names the box just gave. That is the only call needed and it is proven: it turns the reproduced 4 columns back into 5 and puts the figure back in its own cell. The same re-derivation runs once in propose_tables() after a continuation join, so a column blank on page 1 is not missing from the template. It runs ONCE, at the moment the columns are first derived - never again, so a band a person has drawn or moved is never re-derived, which is the rule the register asks for. Finally, the cheap lock, because no derivation can invent a column nobody printed anywhere: doc_table_rows() reports a cell that holds two or more gutter-separated groups of ink, beside `spilled`. A swallowed column is then a number on the screen instead of a silent merge, on both routes.
   - R/tables.R @ doc_columns_from_box(), the block `bands <- .doc_bands(sel, gap)` / `edges <- c(vapply(bands, ...))` (lines 916-930)
       Between `bands` and `edges`, split any band that contains the centres of two or more of `cells` (already computed on line 914) at the midpoint between those cells. The heading row can then never be merged away by what the sampled data rows do or do not print. Keep the naming loop as it is - it maps 
   - app.R @ the `if (m == "cols")` handler, at `d$end <- tryCatch(doc_auto_end(i, d, fr), error = function(e) d$end)` (line 3147)
       Immediately after the end is set, re-derive the divisions once over the whole table: `fit <- tryCatch(doc_fit_columns(i, d, fr), error = function(e) list()); if (length(fit) >= length(.doc_columns(d))) d$columns <- fit`. Names come from the box's columns (doc_fit_columns already re-reads the header 
   - R/tables.R @ doc_fit_columns() (line 938), at `cols <- doc_columns_from_edges(edges, old = .doc_columns(tab))`
       Make this the single source of divisions and make it non-destructive: a column already on `tab` whose band contains no ink in the table must be KEPT as drawn rather than dropped, and the returned set must never have fewer columns than `tab` already has. Apply the same split-by-cells rule as site 1 s
   - R/tables_detect.R @ propose_tables(), the join block at `prev$end <- cd$end` (lines 302-307)
       After the loop that joins continuations (and only for candidates that were joined), re-derive the columns once over the joined extent with doc_fit_columns(), keeping the existing names. A column that prints nothing on the table's first page is then still a column.
   - R/tables_detect.R @ .doc_page_candidates(), `bands <- .doc_bands(body, gap)` (line 165) and the name map `if (j <= length(hdr_cells) ...)` (lines 205-207)
       Apply the same split-by-cells rule to `bands` using the header line's cells, and map a heading to a band by the heading cell's CENTRE (as doc_columns_from_box does) instead of by index - an index map shifts every name one column left the moment the band count and the header cell count differ, which 
   - R/tables.R @ doc_table_rows(), the unclaimed-word block `if (any(is.na(j))) { s <- d[is.na(j), ...] }` (lines 1185-1189)
       Beside `spilled`, count a cell that holds two or more gutter-separated groups of ink (.doc_cells on the words a single band claimed, with the same `gap`), and return it as `crowded` in the result. document_summary()/document_column_report() carry it through, and the builder's status line gets one mo
   - R/wizard_auto.R @ suggest_pdf_columns(), `if (!is.null(hdr) && !anyNA(roles)) {` (line 367)
       Statement parity: recover a money column the heading NAMES but the sampled page never printed a figure in, whether or not every observed column's role could be named. Today the guard drops it into the positional fallback, and the band tiling at 438-466 then hands its space to a neighbour - the same 
RISK: Re-deriving the divisions once, at the \"cols\" gesture, can move a boundary away from what the drag box implied - the person sees the picture redraw in the same gesture, which is acceptable, but it MUST NOT run at any later point or it would overwrite a band that has since been drawn or moved by ha

================================================================
A4-three-line-row [high] REPRODUCED
ROOT CAUSE: `win_pitch` is not the table's line leading. R/tables.R:1074 computes it as `.doc_pitch(w$y, q = 0.25)` - the 25th percentile of the gaps between fixed-3pt y-groups over EVERY word in the window - and R/tables.R:257-258 uses it as the whole yardstick for "one line down" (`gap <= pitch * 1.4`). Because it is a low quantile, the smallest values in that sample win, and two ordinary things put values into the sample that are not leadings at all: (a) `.doc_pitch` groups at the fixed `PARAM_PDF_ROW_TOL` (3) while `.doc_lines` grouped those same words into ONE line at the adaptive `.doc_row_tol` (R/tables.R:303), so any word whose top is 4pt or more off its line's - a larger label beside a smaller value, a superscript, a bullet - injects a phantom sub-line "gap" of 4-8pt; (b) the window is the whole open-ended page, so a footer or footnote block set tighter than the table contributes its own smaller leading. Either one drags `win_pitch` below the table's real leading, the ceiling `pitch * 1.4` falls under it, and EVERY wrap is rejected - so a cell wrapped over three lines comes back as three rows. The predicate is sound; the number handed to it is measured over the wrong population.
FIX: Stop asking a page-wide statistic what "one line down" means, and read it off the two lines themselves. `.doc_is_wrap`'s distance base changes from `pitch` (a quantile over every word in the window) to `h + prev_height` - the type height of the line being tested plus that of the physical line above it. The two existing factors are untouched: `(h + ph) * 1.4` for the indented shape, `(h + ph) * 1.15` for the same-left-edge shape, and all four other guards (n_filled, gap > 0, n_cols >= 2, prev_filled >= 2) stay exactly as they are. In plain words the rule becomes: a wrap is a one-column line sitting under the row with no room for a blank line between them - which is what a person reading the page sees, and it cannot be knocked over by a footnote below the table or by a label set in a bigger face. This is a net DELETION: `win_pitch` at R/tables.R:1074 has no other reader (grep: lines 1074 and 1123 only), so the whole computation and the `pitch` argument go away, replaced by tracking `last_h` alongside the `last_top` that is already tracked. Proven: /tmp/a4/battery2.R variant B2 fixes all four reported-shape cases and ALSO fixes an existing over-fold; the full suite is 6116 pass / 3 fail, and the only two that are mine are a unit-test fixture built without a height column.
   - R/tables.R @ .doc_is_wrap() - the whole function at lines 253-259, plus its comment block at 213-252
       Signature: drop `pitch`, add `prev_height = NA_real_` as the last argument. Body: keep `if (!is.finite(prev_left) || !is.finite(gap)) return(FALSE)` and `if (n_filled > 1L || gap <= 0) return(FALSE)`. Then `h <- suppressWarnings(max(as.numeric(d$height %||% NA_real_), na.rm = TRUE)); ph <- .doc_num(
   - R/tables.R @ line 1074, `win_pitch <- .doc_pitch(w$y, q = 0.25)` inside doc_table_rows()
       Delete the line. It has no other reader - grep for win_pitch returns exactly 1074 and 1123.
   - R/tables.R @ line 1079, `last_top <- NA_real_` (and its comment at 1076-1078)
       Add `last_h <- NA_real_` immediately after it. Same idea as last_top and it must stay exactly parallel to it: the height of the previous PHYSICAL line, folded or kept.
   - R/tables.R @ lines 1119-1123, the `.doc_is_wrap(d, prev_left, ..., win_pitch, n_filled, length(cols), prev_filled)` call
       Drop `win_pitch` and pass `last_h` as the new last argument: `.doc_is_wrap(d, prev_left, <the same gap expression, unchanged>, n_filled, length(cols), prev_filled, last_h)`.
   - R/tables.R @ line 1132, `last_top <- as.numeric(attr(d, "top"))` immediately before the `next` that ends the fold branch
       Add `last_h <- suppressWarnings(max(as.numeric(d$height), na.rm = TRUE))` beside it, so a second and third wrap line measure against the line they actually follow.
   - R/tables.R @ line 1201, `last_top <- as.numeric(attr(d, "top")); prev_filled <- as.integer(n_filled)` at the end of the keep-a-row block
       Add `last_h <- suppressWarnings(max(as.numeric(d$height), na.rm = TRUE))`. These two sites are the only places last_top is assigned; last_h must be assigned at both and nowhere else.
   - R/tables_detect.R @ lines 114-116, the `.doc_is_wrap(lines[[i]], min(lines[[last]]$x), tops[i] - tops[last], pitch, ncell[i])` call inside .runs() in .doc_page_candidates()
       Drop the `pitch` argument and pass the previous line's height: `.doc_is_wrap(lines[[i]], min(lines[[last]]$x), tops[i] - tops[last], ncell[i], 2L, 2L, suppressWarnings(max(as.numeric(lines[[last]]$height), na.rm = TRUE)))`. Keep the local `pitch` variable - line 109 still uses it for `adjacent`. Thi
   - tests/testthat/test-doc_hard.R @ the test_that block "the wrap rule itself answers each shape correctly", lines 684-700
       MUST be updated or it fails - its fixtures are `data.frame(x = 60, text = "more words")` with no height column, a shape no real caller ever produces. Add `height = 9` to d_same and d_in, drop the `14` pitch argument from all six calls, and pass a prev_height of 9. Keep the same six assertions with g
RISK: Low, and on every shape I could measure the change moves in the safe direction - but three honest caveats. (1) The ceiling is now local, so on a deliberately double-spaced table (leading above about 2.8x the type height for an indented wrap, 2.3x for a same-left-edge one) a wrap that folds today wou

================================================================
B1-open-the-editor [high] REPRODUCED
ROOT CAUSE: There is exactly one place a finished conversion is handed to the screen - run_conversion()'s finish callback (app.R:5115-5117: show_result() then publish_result()) - and it has no notion of "and now open the editor". Every route into an editor in this app is a CLICK observer (.teach_now, .rematch_now, rb_open_template from Admin), so opening the editor is only ever something the user initiates. The consequences on the two routes are the same mechanism seen twice: (a) OTHER never opens an editor after a good convert, and its only on-screen door, cv_teach's tables/form branch (app.R:7605-7615) -> observeEvent(input$cv_goto_report) (app.R:7810-7813), switches tab + radio and does NOT call rb_handoff() or rb_open_template(), so she lands on "Add a template" with rb_need_doc saying "Upload the document above to start" - the document and the template that just read it are both thrown away; (b) STATEMENT has no threshold anywhere (no grep hit in app.R or R/ for a toolkit-opening threshold), and its always-available control cv_rematch is guarded by req(.is_txn_result(res)) (app.R:6776) so it never renders for OTHER, while its action .rematch_now() (app.R:6802-6806) deliberately seeds NULL - so even on the statement route the confident-result door drafts a FRESH template instead of opening the one that read it, i.e. "adjust and re-save" is impossible from a clean result on either route.
FIX: ONE predicate and ONE door, both shared by the two routes; no new user-facing control anywhere.

1. The predicate, at file scope beside the one it reuses:
   .needs_editor(res, min_trust) <- (res$status %in% c("ok","needs_review")) && (!.is_txn_result(res) || !.trust_ok(res$trust$level, min_trust))
   A report/form carries NO trust at all (proven: res$trust is NULL on a tables result), so the OTHER half falls out of the same line for free and is honest about why: nothing about a report is proven, so it always opens. The !.is_txn_result clause stays explicit so setting the threshold to "any" cannot switch OTHER off - the user asked for a threshold for STATEMENTS only.
   The status clause is load-bearing: an `unsupported` statement is trust `low` and MUST NOT auto-open the statement toolkit, because cv_teach's card on that result is still asking "is this a statement or something else?" and auto-opening would pre-empt exactly the question B1's other half exists to protect.

2. The threshold: config `convert.open_editor_below_trust`, default "medium", values high | medium | any, evaluated by the EXISTING .trust_ok(). medium = only a `low` statement opens the toolkit by itself; high = medium and low do; any = never opens by itself. Same three words as feed.min_trust, so nobody learns a second vocabulary.

3. The action, one function dispatching on what the result IS, copying Admin's proven dispatch: .edit_now() - kind "tables" -> rb_open_template(t, src$path) with t looked up by res$template_id (round-trip PROVEN identical; rb_editing(id) is set so the header already reads "Editing the saved template X - Save replaces it", app.R:2723-2728/2740; rb_handoff(path) puts the document she just converted under the boxes); kind "form" -> rb_handoff(path) + the report builder on the document (a mode:fields template has no coordinates and no editor - Admin says so at app.R:2307-2312 - and the builder's label/value `pairs` are the pointing-based successor, so the notification must say it will make a report template); statement -> open_guided(path, name, seed_tmpl = the matched template, upload_id) - the seeding rule .teach_now() already uses for ok/needs_review (app.R:7862-7871).

4. The always-available control is cv_rematch, widened rather than added to, and renamed cv_edit: it already sits directly under uiOutput("cv_downloads") (app.R:516), already renders on `ok` as one quiet line, and already has two branches keyed on .match_is_thin(res). Keep both; change only (i) the guard, so it renders for all three kinds, and (ii) the quiet branch's words and action - "Adjust how this was read" calling .edit_now() (seeded), while the thin/ambiguous branch keeps today's wording and .rematch_now() (unseeded). Deleting cv_teach's tables/form branches and observeEvent(input$cv_goto_report) REMOVES two half-working controls, so the count of controls on screen goes down.

HOW THE MODAL-OVER-THE-DOWNLOAD IS AVOIDED, concretely: (a) the OTHER route adds no modal at all - rb_open_template() is a tab switch, and cv_res/cv_src/res$outputs are untouched by it, so clicking back to Convert restores the finished result with its live Download buttons; the auto-open carries one self-dismissing showNotification naming that fact, not a wall. (b) the STATEMENT route is the only one that uses a modal (open_guided -> showModal, Cancel already in its footer at app.R:7489), and it is gated to `low` confidence by default - it only interrupts where the figures are not to be trusted and the download is the wrong thing to want. (c) The auto-open fires from run_conversion()'s finish callback only when `record` is TRUE. That one existing flag already separates "a person just handed the tool a file" (app.R:5476, the Convert button) from the three re-runs that must stay silent: the bundled sample (5499), convert_with_template's forced re-convert (7964) and the post-save re-convert after the toolkit saves a template (8623) - without it, saving a template that still reads at trust low would re-open the toolkit she just closed. (d) A case folder never auto-opens anything: run_batch() does not go through run_conversion(), and opening a batch row is browsing, not converting.
   - app.R @ line 239, `.is_txn_result <- function(res) !isTRUE((res$kind %||% "") %in% c("form", "tables"))`
       Add `.needs_editor(res, min_trust)` immediately after it, at file scope (NOT inside server()) so test-app-ui.R's .ui_fun and test-doc_app.R can lift and actually call it. Body: isTRUE((res$status %||% "") %in% c("ok","needs_review")) && (!.is_txn_result(res) || !.trust_ok(res$trust$level, min_trust)
   - app.R @ lines 5116-5117, `show_result(res, list(path = src, name = name), upload_id %||% uid)` / `publish_result(res, record)` inside run_conversion()'s finish callback
       Add one line after publish_result(): `if (isTRUE(record) && .needs_editor(res, EDITOR_MIN_TRUST)) .edit_now()`. The comment must state why `record` is the gate (it already distinguishes a real upload from the sample, the forced re-convert and the post-save re-convert) and that run_batch() deliberate
   - app.R @ line 6802, `.rematch_now <- function() {`
       Add `.edit_now <- function()` beside it: reads cv_res()/cv_src(); dispatches on res$kind - "tables" -> t <- all_doc_templates()[[tid]]; if non-NULL rb_open_template(t, src$path) else rb_handoff(src$path) + updateRadioButtons(ts_doctype,"other") + updateTabsetPanel("Add a template"); "form" -> the sa
   - app.R @ line 5926, `all_field_templates <- reactive({`
       Add the missing non-admin sibling `all_doc_templates <- reactive({ tpl_bump(); safe(load_document_templates(DOC_DIR, USER_DOC_DIR), list()) })`. Only all_doc_templates_admin() (app.R:1427, include_hidden = TRUE) exists today, and .edit_now must not reach the admin-scoped set.
   - app.R @ lines 6775-6776, `output$cv_rematch <- renderUI({ ... req(.is_txn_result(res))`
       Rename the output to cv_edit and delete the req(.is_txn_result(res)) guard so it renders for statement, form and tables. Keep the two existing branches: the quiet branch (st == "ok", or needs_review with !.match_is_thin) becomes one link actionLink("cv_edit_go", "Adjust how this was read") -> .edit_
   - app.R @ line 516, `uiOutput("cv_rematch"),`
       Rename to uiOutput("cv_edit"). Position is already correct - directly under uiOutput("cv_downloads") - so the door sits beside the payoff on both routes and nothing moves.
   - app.R @ lines 7605-7615 (the `if (identical(res$kind, "form"))` and `if (identical(res$kind, "tables"))` early returns inside output$cv_teach) and lines 7810-7813 (observeEvent(input$cv_goto_report))
       Delete both, plus observeEvent(input$cv_goto_templates) at 7804-7805 if nothing else uses that id. cv_edit is the one door for those two kinds and it carries the document and the template; leaving these in would be two controls for one job, one of which loses the file.
   - R/config.R @ `.config_defaults()` at line 16, after the `app = list(...)` block (ends line 33)
       Add `convert = list(open_editor_below_trust = "medium")` with a comment stating: high | medium | any; the toolkit opens by itself when a STATEMENT's confidence is below this (medium = only `low` opens it; any = never opens by itself); a report or form has no trust to measure and always opens, and th
   - config/config.example.yaml @ after the `app:` block, before `paths:`
       Add the `convert:` block with `open_editor_below_trust: medium` and the same three-line explanation, so the threshold is a stated deployment setting rather than a number buried in the code - B1's explicit requirement.
   - R/forms.R @ lines 329 (`res <- fr`) and 362 (`res <- tr`)
       Carry the person's own answer across the handover: fr$asked_kind <- kind / tr$asked_kind <- kind. Proven dropped: convert_document(kind="other") on a report returns res$asked_kind empty while res$run_log$asked_kind survives. Not required by .needs_editor (which keys on res$kind), but the door's word
RISK: Three real ones. (1) SHADOWING: rb_save (app.R:4395-4409) always writes to USER_DOC_DIR with the id unchanged, and load_document_templates (R/doc_extract.R:174-185) keeps the FIRST match with the curated dir first - so auto-opening a CURATED report template and saving her edit would leave the curate

================================================================
C1-admin-split-and-feedback [high] REPRODUCED
ROOT CAUSE: Admin is organised by DATA SOURCE, not by ROUTE: the template library and the feedback log are each rendered as one un-partitioned frame, on two different tabs, and neither frame carries a route column -- because no single route label exists anywhere in the codebase. library_overview() (R/templates.R:537) emits a three-value `kind` LABEL, submit_feedback() (R/feedback.R:32-44) emits no kind at all, and the run log -- the only place the document name and the outcome kind live -- is never joined to feedback on the run_id both already share. One missing thing produces both faults: a route key (statement | other) derived once at the boundary, plus the feedback->runs join that carries the document name.
FIX: Introduce ONE route label (statement | other) and use the same one twice.

(1) library_overview() gains a leading `route` column derived from template_kind(); the existing sort already puts statement, then fields, then document, so the order needs no change. `kind` stays as the second column so "Other <c2><b7> form" / "Other <c2><b7> report" is not lost inside the band.

(2) adm_tpl_overview is rendered as the SAME single table with DT's RowGroup on column 0 and orderFixed on column 0, column 0 hidden. That produces two headed bands, 'Bank statement' and 'Other', both on screen at once. It adds no control, no second output, no second observer, and -- proven in Chromium -- leaves input$adm_tpl_overview_rows_selected as the data-frame row index, so app.R:1688's `ov$id[i]` is untouched.

(3) A new pure function feedback_overview(feedback, runs, templates) in R/analytics.R joins feedback to the run log on run_id and to the template library on template_id, returning one row per rating: route, when, document, template, verdict, comment, who, template_id, run_id. route comes from template_kind() of the named template; when that template has since been deleted it falls back to the run's own `kind` (statement -> 'Bank statement'; form/tables -> 'Other'); when neither is available it is 'Not recorded' and gets its OWN third band -- never folded into a route it might not belong to. document is the run's source_file, printed as 'not recorded' when the run record is gone, the same rule app.R:8688 already applies to the gaps table. asked_kind is deliberately NOT used.

(4) That table is rendered directly beneath the templates table on the Templates tab, with the identical RowGroup options, so the two tables read as one screen split the same way -- that is the 'same area'. Clicking a feedback row selects that template in the adm_tpl_pick picker already below, so 'traceable to the template' is a route to the template, not a printed id.
